/**
 * OPAC Widget Entry Point
 *
 * This is a minimal bundle containing only the OPAC events widget component.
 * It's separate from the main staff bundle to keep the OPAC bundle size small.
 */

import OpacEventsWidget from "./components/OpacEventsWidget";

export default {
    OpacEventsWidget,
};

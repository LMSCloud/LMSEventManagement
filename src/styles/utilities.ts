import { css } from "lit";

export const utilityStyles = css`
  .pointer-events-none {
    pointer-events: none;
  }

  .w-inherit {
    width: inherit;
  }

  .h-inherit {
    height: inherit;
  }
`;
